pipeline{
       agent any
       stages {
       
            stage('Compile'){
                  steps{ 
                    echo "Compiled successfully"
                               }        
                  }
            stage('to deploy container'){
                 docker      
                  steps{ 
                    sh "docker container run -itd ubuntu:latest"
                    }
                    }
          }
          }
